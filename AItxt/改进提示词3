我现在要进入交易逻辑开发阶段。请阅读当前项目代码后，完成一轮“交易相关功能完整跑通”的迭代。目标不是继续做 UI 演示，而是让现有页面上的交易、委托、资金、持仓、提醒、流水等功能都能实际工作。

请优先保证功能闭环、数据一致性和用户交互完整，不要引入复杂 UI 框架，也不要实现过于复杂的真实撮合系统。

## 总目标

完成一个可运行的测试版交易客户端：

1. 买入委托可以提交。
2. 卖出委托可以提交。
3. 委托会真实保存到测试数据库。
4. 买入时正确冻结资金。
5. 卖出时正确校验持仓和冻结可卖数量。
6. 可撤委托可以撤单。
7. 撤单后释放冻结资金或冻结股票。
8. 满足成交条件的委托可以产生成交回报。
9. 成交后资金、持仓、委托状态、成交记录、资金流水、证券流水都同步更新。
10. 首页、交易页、委托成交页、资产页、行情页、提醒页显示真实数据，不再只显示静态演示数据。

## 一、后端/数据库

请补齐测试版交易后端。优先使用现有 `trading-db.json` 作为统一数据源。如果项目里已经有资金/持仓后端，请整合或兼容；如果没有，请新增一个简单 Node 后端，例如 `trading-server.cjs`，使用 Node 内置 `http/fs/path` 即可，不要引入 Express。

建议端口：`3005`。如果项目已有约定，请按现有约定调整。

### 数据库扩展

在 `trading-db.json` 中保留现有账号、资金、持仓结构，并扩展：

* `orders`
* `fills`
* `cashFlows`
* `stockFlows`
* `alerts`
* `loginRecords`
* 必要时增加 `passwords` 或简单密码字段

不要把数据只放在前端 localStorage。测试后端要真正读写 JSON 文件，保证刷新页面后数据仍然存在。

### 必要接口

请实现或补齐这些接口，命名可以按项目风格调整，但前后端要统一：

* `GET /account?account=admin`

  * 返回资金、持仓、市值、总资产、更新时间。
* `GET /funds?account=admin`

  * 返回可用资金、冻结资金、证券市值、资产总值。
* `GET /holdings?account=admin`

  * 返回持仓明细，包含代码、名称、持仓数量、可卖数量、冻结数量、成本价、最新价、市值、盈亏、盈亏率。
* `GET /orders?account=admin`

  * 返回委托列表。
* `POST /orders`

  * 提交买入/卖出委托。
* `POST /orders/:id/cancel`

  * 撤销未成交或部分成交委托。
* `GET /fills?account=admin`

  * 返回成交回报。
* `GET /cash-flows?account=admin`

  * 返回资金流水。
* `GET /stock-flows?account=admin`

  * 返回证券流水。
* `GET /alerts?account=admin`
* `POST /alerts`
* `PATCH /alerts/:id`
* `DELETE /alerts/:id`
* `POST /funds/deposit`
* `POST /funds/withdraw`
* `POST /passwords/trade`
* `POST /passwords/withdraw`

如果已有类似接口，请复用，不要重复造冲突接口。

## 二、交易业务规则

不用做完整真实交易所撮合，但要有清楚、可解释、稳定的测试版规则。

### 买入委托

提交买入时校验：

1. 股票代码必须存在。
2. 价格必须大于 0。
3. 数量必须大于 0，最好是 100 的整数倍；如果当前 UI 没有限制，至少要提示。
4. 委托金额 = 委托价 × 数量。
5. 委托金额不能超过可用资金。
6. 委托价不能超出涨跌停区间。

提交成功后：

* 生成委托号，例如 `ORD-时间戳`。
* 写入 orders。
* 从可用资金扣除委托金额。
* 增加冻结资金。
* 写入资金流水：`买入委托冻结`。
* 如果满足成交条件，则立即成交或部分成交，并更新资金、持仓、成交、流水。
* 如果不满足成交条件，状态为 `未成交`。

### 卖出委托

提交卖出时校验：

1. 股票代码必须存在。
2. 股票必须在持仓中。
3. 价格必须大于 0。
4. 数量必须大于 0。
5. 数量不能超过可卖数量。
6. 委托价不能超出涨跌停区间。

可卖数量 = 持仓总数 - 当前未成交/部分成交卖出委托的未成交数量。

提交成功后：

* 生成委托号。
* 写入 orders。
* 不直接改变资金。
* 卖出委托的未成交数量视为冻结股票。
* 写入证券流水或冻结说明。
* 如果满足成交条件，则成交并减少持仓、增加可用资金、写成交回报和流水。
* 如果不满足成交条件，状态为 `未成交`。

### 成交规则

测试版使用简单规则即可：

* 买入：如果委托价 >= 当前卖一价，则成交。
* 卖出：如果委托价 <= 当前买一价，则成交。
* 成交价：

  * 买入可使用当前卖一价。
  * 卖出可使用当前买一价。
* 如果没有买一/卖一，则使用最新价。
* 可以先默认全部成交。
* 如想支持部分成交，可做简单规则：数量大于 500 时成交一部分，其余保持 `部分成交`。但不要让逻辑变复杂。

成交后必须同步：

* order.status
* order.filledQuantity
* order.avgPrice
* fills
* balances.available
* balances.frozen
* positions
* cashFlows
* stockFlows

### 撤单规则

只允许撤销：

* `未成交`
* `部分成交`

不允许撤销：

* `已成交`
* `已撤单`
* `已过期`
* `已拒绝`

买入撤单：

* 释放未成交数量对应的冻结资金。
* 状态改为 `已撤单`。
* 写入资金流水：`撤单解冻`。

卖出撤单：

* 状态改为 `已撤单`。
* 释放未成交股票数量。
* 写入证券流水：`撤单解冻`。

## 三、前端统一数据层

请新增或重构一个统一数据访问层，例如：

* `src/services/tradingApi.ts`
* `src/composables/useTradingStore.ts`

不要让每个页面各自 fetch 和维护一套不一致数据。

需要提供这些方法：

* `refreshAccount()`
* `refreshFunds()`
* `refreshHoldings()`
* `refreshOrders()`
* `refreshFills()`
* `refreshFlows()`
* `refreshAlerts()`
* `placeOrder(payload)`
* `cancelOrder(orderId)`
* `deposit(amount)`
* `withdraw(amount, password?)`
* `createAlert(payload)`
* `updateAlert(id, payload)`
* `deleteAlert(id)`
* `changeTradePassword(payload)`
* `changeWithdrawPassword(payload)`

可以不用 Pinia。如果项目没有全局 store，就用 Vue composable + module-level reactive state 即可。

要求：

* 所有交易相关页面共用同一份数据。
* 下单成功后自动刷新资金、持仓、订单、成交和流水。
* 撤单成功后自动刷新资金、持仓、订单和流水。
* 页面刷新后从后端重新加载真实数据。
* 不要继续让按钮只弹提示或只改本地假数据。

## 四、TradeView / TradeTicket

重点改这里。当前交易页和 `TradeTicket.vue` 还偏静态，请改成真实可用。

### 页面行为

交易页需要支持：

* 买入/卖出切换。
* 从行情页跳转时读取 query：`symbol` 和 `side`。
* 输入股票代码后加载股票详情。
* 自动带出最新价、买一、卖一、涨停价、跌停价。
* 买入默认参考价可以填卖一价或最新价。
* 卖出默认参考价可以填买一价或最新价。
* 显示可用资金。
* 显示最大可买数量。
* 显示持仓数量、可卖数量。
* 校验错误实时显示。
* 预览委托。
* 确认提交。
* 提交成功后显示委托号和状态。
* 提供“查看委托”按钮跳转到委托成交页。

### 表单字段

保留：

* 股票代码
* 委托方向
* 委托类型：限价
* 价格
* 数量
* 有效期：当日有效
* 备注

暂时不要开放市价、止损等复杂类型。如果 UI 里有这些选项，请先禁用或移除，避免用户以为已实现。

### 校验

前端和后端都要校验，不要只靠前端。

前端校验包括：

* 股票不存在。
* 价格为空或无效。
* 数量为空或无效。
* 买入金额超过可用资金。
* 卖出数量超过可卖数量。
* 价格超出涨跌停区间。
* 账户状态异常时禁止提交。

## 五、OrdersView / OrderListTable

委托成交页要接真实数据。

### OrdersView

实现：

* 加载真实 orders。
* 加载真实 fills。
* 状态筛选：

  * 全部
  * 可撤
  * 未成交
  * 部分成交
  * 已成交
  * 已撤单
  * 已拒绝
* 搜索委托号/股票代码/股票名称。
* 撤单。
* 批量撤单。
* 刷新委托。
* 显示成交回报。
* 操作成功后刷新数据。

### OrderListTable

如果还没有，请补齐：

* 通过 props 接收订单。
* 不要内部写死订单。
* 支持选择行，用于批量撤单。
* 支持撤单事件。
* 显示：

  * 委托号
  * 时间
  * 股票
  * 方向
  * 委托价
  * 委托量
  * 已成交
  * 未成交
  * 成交均价
  * 状态
  * 操作

## 六、DashboardView

首页要显示真实交易数据。

需要接入统一 store：

* 资金概览来自真实 funds。
* 持仓来自真实 holdings。
* 今日待办来自真实 orders：

  * 未成交
  * 部分成交
  * 可撤单
  * 已触发提醒
* 市场脉动来自行情服务。
* 最新委托来自真实 orders。
* 最新成交来自真实 fills。

“新建委托”按钮跳转到 `/trade`。

## 七、AccountView

如果项目中有 `AccountView.vue`，请接入真实数据：

* 资金概览来自后端。
* 持仓表来自后端。
* 存款按钮实际调用 `deposit`。
* 取款按钮实际调用 `withdraw`。
* 资金流水来自 `cashFlows`。
* 证券流水来自 `stockFlows`。
* 买入成交、卖出成交、撤单解冻后，刷新资产页能看到变化。

取款校验：

* 金额必须大于 0。
* 金额不能超过可用资金。
* 如已有取款密码字段，请校验；如果没有，可以先做简单密码校验或留后端判断。

## 八、MarketView / StockDetailCard / PriceTicker

行情页主要接已有 `market-server.cjs`，但要把操作按钮接通：

* 行情表点击买入：跳转 `/trade?symbol=xxx&side=buy`
* 行情表点击卖出：跳转 `/trade?symbol=xxx&side=sell`
* 设置提醒：跳转 `/alerts?symbol=xxx`
* 股票详情里的买入/卖出/提醒按钮也要接通。
* 自选、刷新、搜索如果已有 UI，也要实际生效。

不要让按钮没有响应。

## 九、AlertsView

提醒页如果已有，请接入真实后端或统一 store：

* 新增提醒实际保存。
* 暂停/恢复实际更新。
* 删除实际生效。
* 从 `/alerts?symbol=xxx` 进入时，自动带入股票代码。
* 刷新行情时检查提醒条件：

  * 当前价 >= 高于目标价，触发。
  * 当前价 <= 低于目标价，触发。
* 触发后状态改为 `已触发`，显示触发时间。
* 首页今日待办里的“已触发提醒”要能反映真实数量。

## 十、SettingsView / LoginView

如果页面中有未完成按钮，请补齐最小可用逻辑：

* 修改交易密码：校验当前密码、新密码、确认密码。
* 修改取款密码：同上。
* 验证证书：调用现有证书逻辑，不要破坏已有 `certificateStore.ts`。
* 登录成功后写入登录记录。
* 登录记录页显示真实 loginRecords。

不需要做复杂安全系统，但按钮不能只是摆设。

## 十一、数据一致性要求

请特别注意这些一致性：

1. 买入未成交：

   * 可用资金减少。
   * 冻结资金增加。
   * 委托显示未成交。
   * 持仓不增加。

2. 买入成交：

   * 冻结资金减少。
   * 持仓增加。
   * 成交回报增加。
   * 资金流水增加。
   * 证券流水增加。
   * 委托状态变为已成交或部分成交。

3. 买入撤单：

   * 未成交部分冻结资金释放。
   * 委托状态变为已撤单。

4. 卖出未成交：

   * 可卖数量减少。
   * 持仓总数不变。
   * 委托显示未成交。

5. 卖出成交：

   * 持仓减少。
   * 可用资金增加。
   * 成交回报增加。
   * 流水增加。
   * 委托状态更新。

6. 卖出撤单：

   * 未成交部分释放为可卖。
   * 委托状态变为已撤单。

## 十二、错误和空态

所有页面都要有基本反馈：

* 加载中
* 请求失败
* 空数据
* 校验错误
* 提交成功
* 撤单成功
* 后端拒绝原因

不要吞掉错误，也不要假装成功。

## 十三、不要做的事

* 不要引入大型 UI 框架。
* 不要引入复杂状态管理，除非项目已经有。
* 不要实现完整真实交易所撮合。
* 不要把功能继续写成纯 localStorage 演示。
* 不要让按钮只 alert 或 console.log。
* 不要破坏当前已经设计好的 UI 风格。
* 不要大改登录证书逻辑。
* 不要让前端和后端各自维护不一致的数据。

## 十四、建议实现顺序

1. 先实现/扩展 `trading-server.cjs` 和 `trading-db.json`。
2. 实现 `tradingApi.ts`。
3. 实现 `useTradingStore.ts` 或等价统一数据层。
4. 接通 `TradeView` 和 `TradeTicket`。
5. 接通 `OrdersView` 和 `OrderListTable`。
6. 接通 `DashboardView`。
7. 接通 `AccountView`。
8. 接通 `MarketView` 的买入/卖出/提醒跳转。
9. 接通 `AlertsView`。
10. 接通 `SettingsView` 的密码修改和登录记录。
11. 最后做一次完整流程测试。

## 十五、最终验收流程

请确保下面流程能真实跑通：

1. 启动行情后端和交易后端。
2. 登录账号 admin。
3. 首页显示真实资金和持仓。
4. 进入行情页，选择一只股票，点击买入。
5. 交易页自动带入股票代码和参考价格。
6. 输入数量，预览委托。
7. 提交买入委托。
8. 委托成交页出现该委托。
9. 如果未成交，可以撤单，资金解冻。
10. 如果成交，成交回报出现，持仓和资金更新。
11. 再提交一笔卖出委托。
12. 卖出后持仓、可卖数量、资金、成交回报正确变化。
13. 资产页能看到资金流水和证券流水。
14. 创建价格提醒后，提醒页能显示、暂停、恢复、删除。
15. 修改交易密码/取款密码有真实校验和反馈。
16. 刷新页面后，订单、成交、资金、持仓数据仍然保留。

请先给出简短修改计划，然后直接修改代码。完成后说明新增/修改了哪些文件，以及如何启动测试后端和前端。
