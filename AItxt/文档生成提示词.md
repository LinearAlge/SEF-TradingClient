请完整阅读当前项目的所有文件结构和源码，然后为本项目生成一套面向开发成员的项目文档。文档不是给最终用户看的，而是给本组开发同学、后续维护同学、以及其他组联调同学看的，所以要尽可能详细、准确、可操作。

不要只写泛泛介绍，必须基于当前真实代码、目录结构、package.json 脚本、前端页面、后端服务、数据库文件、环境变量和 API 调用方式来写。凡是代码里不存在或无法确认的内容，不要编造；请写“当前未发现 / 待确认 / 建议补充”。

## 一、生成目标

请生成或更新这些文档文件：

```text
README.md
docs/PROJECT_GUIDE.md
docs/BACKEND_ARCHITECTURE.md
docs/API_CONTRACT.md
docs/TESTING_GUIDE.md
```

如果项目当前没有 `docs/` 目录，请创建。

各文档分工如下：

* `README.md`：项目入口说明，适合第一次打开项目的人快速启动。
* `docs/PROJECT_GUIDE.md`：完整项目介绍和前端/后端整体说明，最详细。
* `docs/BACKEND_ARCHITECTURE.md`：后端结构、数据归属、mock 服务、未来联调替换方式。
* `docs/API_CONTRACT.md`：当前前端依赖的 API、统一网关 API、mock 外部服务 API、未来其他组需要实现的接口效果。
* `docs/TESTING_GUIDE.md`：测试账号、启动脚本、测试流程、典型业务路径、常见问题。

## 二、文档必须先基于代码分析

写文档前请先检查：

1. `package.json`

   * 项目依赖
   * scripts
   * 启动命令
   * 是否有前端、网关、mock 服务相关脚本

2. 前端目录

   * `src/views`
   * `src/components`
   * `src/services`
   * `src/composables`
   * `src/router`
   * `src/utils`
   * `src/assets`
   * 其他实际存在目录

3. 后端目录

   * `backend/`
   * client 自有服务
   * gateway 服务
   * mock 服务
   * adapters
   * SQLite 相关文件
   * JSON mock 数据

4. 配置文件

   * `.env`
   * `.env.example`
   * Vite 配置
   * TypeScript 配置
   * 端口配置
   * API base URL 配置

5. 数据库

   * SQLite 数据库初始化代码
   * 表结构
   * seed 数据
   * 仍然保留的 JSON mock 数据
   * 哪些数据属于客户端自有数据库
   * 哪些数据只是外部系统 mock

6. API 调用

   * 前端实际调用哪个 API base URL
   * 是否统一走 `clientApi`
   * 是否还存在页面直接访问多个端口的情况
   * 后端 gateway 如何转发到 mock 服务或 adapter

文档必须反映真实项目现状。如果发现代码结构还不完全统一，请在文档里说明“当前状态”和“建议后续整理方向”。

## 三、README.md 内容要求

`README.md` 要简洁但完整，至少包含：

1. 项目名称和定位

   * 股票交易系统交易客户端
   * 本项目负责交易客户端前端、客户端自有后端、测试 mock 服务、联调网关

2. 项目实现范围

   * 登录
   * 首次证书绑定/验证
   * 首页工作台
   * 行情查询
   * 交易下单
   * 委托成交
   * 资产/资金/持仓
   * 提醒
   * 安全设置
   * 测试后端与 mock 服务

3. 技术栈

   * 前端框架
   * 构建工具
   * 后端运行环境
   * 数据库/存储方式
   * 主要依赖

4. 快速启动

   * 安装依赖
   * 初始化数据库，如果需要
   * 启动客户端自有后端
   * 启动 mock 服务
   * 启动前端
   * 访问地址

5. 测试账号

   * 账号
   * 密码
   * 交易密码
   * 取款密码
   * 首次登录/证书相关说明

6. 常用脚本

   * 每个 npm script 的作用
   * 哪些脚本必须先启动
   * 哪些是可选 mock 服务

7. 文档入口

   * 指向 `docs/PROJECT_GUIDE.md`
   * 指向 `docs/BACKEND_ARCHITECTURE.md`
   * 指向 `docs/API_CONTRACT.md`
   * 指向 `docs/TESTING_GUIDE.md`

## 四、PROJECT_GUIDE.md 内容要求

这是最详细的总文档，要求尽量完整。

请包含：

### 1. 项目背景

说明本项目来源于股票交易系统实验，交易客户端需要完成：

* 客户端申请
* 首次登录
* 登录
* 查看股票价格
* 查询资金账户现金
* 查询证券账户持仓
* 发出买入/卖出委托
* 撤销委托
* 显示交易结果
* 修改密码
* 价格提醒

同时说明完整系统还包括其他模块：

* 证券账户业务
* 资金账户业务
* 中央交易系统
* 网上信息发布
* 交易系统管理

强调本项目是交易客户端，当前为了独立开发和演示，提供了 mock 外部服务。

### 2. 项目整体架构

请根据实际代码画出文字版架构图，例如：

```text
前端 Vue 应用
  ↓
src/services/clientApi.ts
  ↓
客户端统一网关 client-gateway-server
  ↓
adapters
  ├─ client auth / client state / SQLite
  ├─ mock funds service
  ├─ mock securities service
  ├─ mock exchange service
  └─ mock market service
```

如果实际结构不同，请按实际结构写。

### 3. 前端结构说明

按目录解释：

* views 每个页面负责什么
* components 主要组件负责什么
* services 数据请求如何封装
* composables 是否有全局状态
* router 路由结构
* assets 资源说明

每个页面请说明：

* 登录页
* 首页
* 行情页
* 交易页
* 委托成交页
* 资产页
* 提醒页
* 安全页
* 404 页

每个页面要写清：

* 页面用途
* 依赖哪些 API
* 主要交互
* 目前已完成能力
* 仍然依赖 mock 的地方

### 4. 后端结构说明

说明当前后端分为：

* 客户端自有服务
* 客户端统一网关
* 外部业务 mock 服务

明确解释：

* 哪些服务是本组正式负责
* 哪些只是为了独立跑通前端的 mock
* 后期如何替换成其他组服务

### 5. 数据流说明

至少说明这些流程：

* 登录与证书验证流程
* 首页加载数据流程
* 行情查询流程
* 从行情页跳转交易页流程
* 买入委托流程
* 卖出委托流程
* 撤单流程
* 成交回报刷新流程
* 资产/持仓/流水刷新流程
* 提醒新增、触发、暂停、删除流程
* 修改密码流程

每个流程尽量写成步骤，不要只写一句话。

### 6. 当前限制和后续替换点

说明当前 mock 或简化的地方，例如：

* 撮合逻辑是测试版
* 行情数据是模拟数据
* 资金/持仓数据是 mock
* 安全证书是实验版实现
* SQLite 或 JSON 数据只适合本地测试
* 后期联调需要替换 adapters 或 base URL

## 五、BACKEND_ARCHITECTURE.md 内容要求

这份文档要重点讲清楚后端边界。

请包含：

### 1. 后端设计原则

写清楚：

* 前端不直接依赖多个业务服务
* 前端统一调用客户端网关
* 客户端组只正式拥有客户端自有数据
* 资金、持仓、撮合、行情等权威数据属于其他组
* 当前 mock 服务用于独立开发、演示和联调前测试

### 2. 数据归属表

请生成表格，至少包含：

| 数据类型 | 当前实现位置 | 当前存储 | 正式归属 | 是否本组正式维护 | 后期替换方式 |
| ---- | ------ | ---- | ---- | -------- | ------ |

需要覆盖：

* 用户登录
* 安全证书
* 登录记录
* 客户端申请
* 价格提醒
* 通知消息
* 自选股/偏好
* 资金余额
* 冻结资金
* 资金流水
* 持仓
* 可卖数量
* 证券流水
* 委托
* 成交回报
* 行情
* 公告
* 涨跌停规则
* 停牌状态

### 3. SQLite 数据库说明

如果项目已使用 SQLite，请写：

* 数据库文件路径
* 初始化方式
* 表结构
* 每张表用途
* seed 数据
* 如何重置数据库
* 注意不要提交哪些运行时数据库文件，或者是否允许提交测试数据库

如果项目没有 SQLite 或未完成，请按当前实际情况写，并说明建议。

### 4. Mock 服务说明

逐个说明：

* mock funds service
* mock securities service
* mock exchange service
* mock market service

每个服务写：

* 作用
* 端口
* 数据源文件
* 提供的主要接口
* 模拟了哪个外部组
* 后续替换目标

### 5. Adapter 设计说明

说明每个 adapter 的作用：

* authAdapter
* fundsAdapter
* securitiesAdapter
* exchangeAdapter
* marketAdapter

写清楚后续如果其他组提供真实接口，应该改哪里，不应该改前端页面。

## 六、API_CONTRACT.md 内容要求

这份文档要写成接口契约，方便其他组看。

请包含：

### 1. 前端统一调用的客户端 API

列出当前前端实际应该调用的 API：

* auth
* account
* market
* trade
* alerts
* notifications
* preferences/watchlist

每个接口写：

* 方法
* 路径
* 查询参数
* 请求体
* 响应体示例
* 错误响应示例
* 备注

示例格式：

````md
### POST /api/trade/orders

用途：提交买入/卖出委托。

Request Body:
```json
{
  "account": "admin",
  "symbol": "600001",
  "side": "买入",
  "price": 100.5,
  "quantity": 100,
  "note": ""
}
````

Response:

```json
{
  "success": true,
  "order": {
    "id": "ORD-...",
    "status": "未成交"
  }
}
```

````

### 2. 外部组未来需要实现的接口效果

按组拆分：

#### 资金账户组

需要实现或等价提供：

- 查询资金
- 查询资金流水
- 存款
- 取款
- 冻结资金
- 解冻资金
- 买入成交扣款
- 卖出成交入账
- 修改交易密码/取款密码

#### 证券账户组

需要实现或等价提供：

- 查询持仓
- 查询证券流水
- 查询账户状态
- 卖出冻结
- 撤单解冻
- 买入入股
- 卖出出股

#### 中央交易系统组

需要实现或等价提供：

- 提交委托
- 撤单
- 查询委托
- 查询成交
- 返回未成交、部分成交、已成交、已撤单等状态
- 返回成交均价、已成交数量、未成交数量

#### 网上信息发布组

需要实现或等价提供：

- 股票列表
- 股票详情
- 股票代码/名称模糊查询
- 最新价
- 买一/卖一
- 日/周/月最高最低
- 公告
- 5 秒级行情刷新

#### 交易系统管理组

需要实现或等价提供：

- 涨跌停规则
- 停牌/恢复交易状态
- 股票交易状态

### 3. 字段约定

列出统一字段命名，例如：

- account
- fundAccountId
- securitiesAccountId
- symbol
- name
- side
- price
- quantity
- filledQuantity
- avgPrice
- status
- available
- frozen
- totalShares
- availableShares
- frozenShares
- lastPrice
- bid
- ask

说明方向、状态、金额、时间格式的约定。

### 4. 状态枚举

列出：

- 委托状态
- 提醒状态
- 账户状态
- 股票状态
- 消息状态

### 5. 错误码或错误格式

如果当前代码有统一错误格式，请按实际写。
如果没有，请提出当前项目建议使用格式：

```json
{
  "success": false,
  "message": "可用资金不足",
  "code": "INSUFFICIENT_FUNDS"
}
````

## 七、TESTING_GUIDE.md 内容要求

这份文档要让其他开发者照着就能跑起来并测试完整流程。

请包含：

### 1. 环境要求

* Node.js 版本
* npm/pnpm/yarn
* SQLite 是否需要本地安装
* 操作系统注意事项

### 2. 安装依赖

写实际命令。

### 3. 环境变量

说明 `.env` 或 `.env.example` 中每个变量作用。

### 4. 启动步骤

按顺序写：

1. 安装依赖
2. 初始化或检查数据库
3. 启动 gateway
4. 启动 mock 服务
5. 启动前端
6. 打开浏览器地址

如果 package.json 有一键启动脚本，也写出来。

### 5. 测试账号

列出当前 seed 的测试账号：

* 登录账号
* 登录密码
* 交易密码
* 取款密码
* 是否首次登录
* 证书绑定注意事项

如果账号信息需要从 seed 或数据库读取，请按实际代码写。

### 6. 完整业务测试流程

必须写这些测试路径：

#### 登录测试

* 普通登录
* 首次登录/证书绑定
* 重新绑定证书
* 登录失败

#### 行情测试

* 查询股票
* 按代码查询
* 按名称模糊查询
* 查看股票详情
* 从行情跳转买入/卖出
* 设置提醒

#### 买入测试

* 进入交易页
* 输入股票代码
* 自动带出参考价
* 输入数量
* 预览委托
* 提交委托
* 查看委托成交页
* 检查资金冻结/成交/流水变化

#### 卖出测试

* 先确保有持仓
* 输入卖出股票
* 校验可卖数量
* 提交卖出委托
* 查看持仓、资金、成交回报变化

#### 撤单测试

* 提交未成交委托
* 在委托页撤单
* 检查状态变为已撤单
* 检查冻结资金或冻结股票释放

#### 资产页测试

* 查看资金概览
* 查看持仓
* 查看资金流水
* 查看证券流水

#### 提醒测试

* 新增提醒
* 暂停提醒
* 恢复提醒
* 删除提醒
* 触发提醒

#### 安全设置测试

* 修改交易密码
* 修改取款密码
* 查看登录记录
* 验证证书状态

### 7. 常见问题

请根据项目实际情况写：

* 端口被占用
* 后端未启动
* 前端白屏
* 数据库未初始化
* 登录失败
* 证书验证失败
* 行情无数据
* 委托提交失败
* SQLite 依赖安装失败
* mock 服务和 gateway 配置不一致

### 8. 如何重置测试数据

写清楚：

* 删除哪个 SQLite 文件
* 删除哪个 JSON mock 文件或恢复 seed
* 运行哪个初始化脚本
* 重启哪些服务

## 八、写作要求

1. 文档可以比较详细，宁可长一点，不要省略或编造关键步骤。
2. 每个命令都用代码块。
3. 每个接口尽量给 JSON 示例。
4. 每个服务都写清楚“当前用途”和“未来替换方式”。
5. 不要写空泛宣传语。
6. 不要写给最终用户看的产品介绍。
7. 不要编造代码中没有的脚本、端口、账号。如果缺少，就标注“当前未配置，建议补充”。
8. 如果发现实际代码和理想架构不一致，要如实写当前现状，并写建议。
9. 最后请输出你新增/修改了哪些文档文件，以及文档里有哪些需要人工确认的 TODO。
