请对当前股票交易客户端项目做一次“后端结构整理 + 客户端自有数据正式化 + 外部服务 mock 化”的迭代。暂时不要生成说明文档，先把代码结构和运行逻辑整理好。

目标：让前端以后只依赖一个客户端统一 API，不再直接散落调用多个测试端口；同时把交易客户端组自己负责的数据从零散 JSON mock 中拆出来，使用更正式的 SQLite 数据库存储。资金、持仓、行情、撮合、成交等不属于客户端组权威数据的内容，继续作为 mock 外部服务保留，方便后期替换成其他组接口。

## 一、总体原则

1. 不要继续让前端页面直接调用多个后端端口。
2. 前端统一调用 `client-gateway-server` 提供的 `/api/...` 接口。
3. 交易客户端组正式拥有的数据使用 SQLite。
4. 其他组正式负责的数据继续用 mock JSON 服务模拟。
5. 不要大改当前 UI。
6. 不要重写已经能工作的交易逻辑，只做结构整理和接口统一。
7. 保持当前功能可运行：登录、证书、行情、资产、交易、委托、提醒等页面不能坏。

## 二、推荐后端结构

请尽量整理成类似结构：

```text
backend/
  client/
    client-gateway-server.cjs
    client-db.cjs
    client-auth-service.cjs
    client-state-service.cjs
    adapters/
      authAdapter.cjs
      fundsAdapter.cjs
      securitiesAdapter.cjs
      exchangeAdapter.cjs
      marketAdapter.cjs

  mocks/
    mock-funds-service.cjs
    mock-securities-service.cjs
    mock-exchange-service.cjs
    mock-market-service.cjs
    data/
      mock-funds-db.json
      mock-securities-db.json
      mock-exchange-db.json
      mock-market-db.json
```

如果完整拆文件成本太高，可以先保持少量文件，但必须在代码层面区分：

* client 自有数据
* 外部资金服务 mock
* 外部证券持仓服务 mock
* 外部中央交易系统 mock
* 外部行情服务 mock

## 三、客户端组自己负责的数据改用 SQLite

请引入 SQLite 作为客户端自有后端数据库。优先使用 `better-sqlite3`，并更新 `package.json`。如果当前项目环境不适合 `better-sqlite3`，可以改用其他轻量 SQLite 方案，但不要继续用 JSON 保存这些正式属于客户端的数据。

SQLite 数据库路径建议：

```text
backend/client/client.sqlite
```

新增 `client-db.cjs`，负责：

* 初始化数据库
* 建表
* seed 测试用户
* 提供简单查询/写入方法

客户端自有表建议包括：

```text
client_users
client_certificates
client_sessions
client_login_records
client_applications
client_alerts
client_notifications
client_preferences
client_watchlist
```

这些数据属于交易客户端组，应保留在自己项目中。

### 必须迁移到 SQLite 的数据

请把当前 `mock-auth-db.json`、`trading-db.json` 里属于客户端自己的数据迁移到 SQLite：

* 登录用户
* 登录密码或测试密码
* 首次登录状态
* 证书公钥
* 登录记录
* 客户端申请记录
* 价格提醒
* 通知消息
* 自选股
* 用户偏好

注意：不要把资金余额、持仓、订单、成交、行情正式迁入客户端 SQLite，它们属于外部业务域，只能作为 mock 继续存在。

## 四、保留为 mock 的外部服务

这些数据不要作为客户端正式数据库，只保留 mock，方便独立跑通前端：

### 1. mock-funds-service

模拟资金账户组：

* 可用资金
* 冻结资金
* 存款
* 取款
* 资金流水
* 交易密码/取款密码校验

### 2. mock-securities-service

模拟证券账户组：

* 证券账户
* 持仓
* 可卖数量
* 冻结股票
* 持仓成本
* 证券流水

### 3. mock-exchange-service

模拟中央交易系统组：

* 提交买入/卖出委托
* 撤单
* 委托状态
* 成交回报
* 简化撮合逻辑

### 4. mock-market-service

模拟网上信息发布组：

* 股票列表
* 股票详情
* 股票代码/名称搜索
* 最新价
* 买一/卖一
* 日/周/月高低
* 公告
* 简单 5 秒刷新价格

现有的 `market-server.cjs` 和 `market-db.json` 可以改造成这个服务，不要删除已有数据。

## 五、client-gateway-server

新增或改造一个统一网关：

```text
backend/client/client-gateway-server.cjs
```

前端以后只调用它。

建议端口：

```text
3010
```

前端统一 base URL：

```text
VITE_CLIENT_API_BASE=http://localhost:3010/api
```

网关提供这些接口：

```text
POST /api/auth/login
POST /api/auth/enroll
POST /api/auth/verify
POST /api/auth/rebind
GET  /api/auth/me

GET  /api/account/summary
GET  /api/account/funds
GET  /api/account/holdings
GET  /api/account/cash-flows
GET  /api/account/stock-flows

GET  /api/market/stocks
GET  /api/market/stocks/:symbol
GET  /api/market/quotes

POST /api/trade/orders
GET  /api/trade/orders
POST /api/trade/orders/:id/cancel
GET  /api/trade/fills

GET    /api/client/alerts
POST   /api/client/alerts
PATCH  /api/client/alerts/:id
DELETE /api/client/alerts/:id

GET   /api/client/notifications
PATCH /api/client/notifications/:id/read

GET  /api/client/watchlist
POST /api/client/watchlist/:symbol/toggle

GET   /api/client/preferences
PATCH /api/client/preferences
```

网关内部通过 adapter 调用不同来源：

* auth/client state：调用 SQLite
* funds：调用 mock-funds-service
* holdings：调用 mock-securities-service
* trade/orders/fills：调用 mock-exchange-service
* market：调用 mock-market-service

以后替换其他组服务时，只改 adapter，不改前端页面。

## 六、前端改造要求

请新增或整理：

```text
src/services/clientApi.ts
```

前端页面和 composable 只允许通过 `clientApi.ts` 调用后端。

请检查并替换现有代码里直接写死的：

```text
localhost:3001
localhost:3002
localhost:3003
localhost:3004
localhost:3005
```

改为统一调用：

```text
VITE_CLIENT_API_BASE
```

如果已有 `tradingApi.ts` 或类似文件，可以保留但改成对 `clientApi.ts` 的封装，不要让页面直接访问多个服务。

## 七、配置方式

请增加环境配置或常量配置：

```text
API_PROFILE=mock
CLIENT_API_PORT=3010
FUNDS_SERVICE_BASE_URL=http://localhost:3021
SECURITIES_SERVICE_BASE_URL=http://localhost:3022
EXCHANGE_SERVICE_BASE_URL=http://localhost:3023
MARKET_SERVICE_BASE_URL=http://localhost:3024
```

mock 模式下 adapter 调用本地 mock 服务。

后期联调时，只需要改这些 base URL，前端不改。

## 八、启动脚本

请在 `package.json` 里补充便于开发的脚本，例如：

```json
{
  "scripts": {
    "dev": "vite",
    "dev:gateway": "node backend/client/client-gateway-server.cjs",
    "dev:mock:funds": "node backend/mocks/mock-funds-service.cjs",
    "dev:mock:securities": "node backend/mocks/mock-securities-service.cjs",
    "dev:mock:exchange": "node backend/mocks/mock-exchange-service.cjs",
    "dev:mock:market": "node backend/mocks/mock-market-service.cjs"
  }
}
```

如果你能用 `concurrently`，可以增加一条总启动脚本；如果不想增加依赖，就只提供单独脚本。

## 九、当前文件处理建议

请按下面方式处理当前已有文件：

* `mock-auth-server.cjs`：改造成客户端正式 auth 逻辑，接 SQLite。
* `mock-auth-db.json`：作为迁移/seed 参考，不再作为正式运行数据源。
* `trading-server.cjs`：拆出或改造成 mock-exchange-service，不再保存客户端提醒/登录记录等数据。
* `trading-db.json`：拆分，客户端自有数据迁移到 SQLite，交易/资金/持仓/成交相关数据保留为 mock 外部数据。
* `funds-server.cjs`：整理成 mock-funds-service。
* `holdings-server.cjs`：整理成 mock-securities-service。
* `market-server.cjs`：整理成 mock-market-service。
* `market-db.json`：保留为 mock-market 数据源。

## 十、功能保持要求

这次结构整理后，以下流程仍应可用：

1. 登录 admin。
2. 首次登录/证书绑定逻辑不坏。
3. 首页能显示资产、持仓、委托、成交。
4. 行情页能查询股票。
5. 行情页买入/卖出按钮能跳转交易页。
6. 交易页能提交委托。
7. 委托页能查看委托、撤单、查看成交回报。
8. 资产页能看资金、持仓、流水。
9. 提醒页能新增、暂停、恢复、删除提醒。
10. 设置页能显示登录记录、修改测试密码或至少保留原逻辑。

如果某个 mock 服务暂时未启动，前端应显示请求失败提示，而不是白屏。

## 十一、不要做的事

* 不要生成说明文档。
* 不要重做 UI。
* 不要引入大型框架。
* 不要把所有业务都塞进一个巨大的 `trading-server.cjs`。
* 不要把客户端正式数据继续保存在 JSON 里。
* 不要把资金、持仓、行情、成交这些外部权威数据放进客户端 SQLite。
* 不要让前端继续直接访问多个后端端口。
* 不要破坏当前已有的交易页面和登录页面。

## 十二、完成后请输出

完成代码修改后，请说明：

1. 新增/修改了哪些后端文件。
2. 哪些数据进入了 SQLite。
3. 哪些服务仍然是 mock 外部服务。
4. 前端现在统一调用哪个 API base URL。
5. 如何启动网关和 mock 服务。
6. 如果后期替换其他组服务，需要改哪个 adapter 或配置。
