你现在要对股票交易系统交易客户端项目做一次“统一 FastAPI 后端化改造”。请严格基于《接口V2_修正版.md》和《交易客户端_设计报告(1).md》，在保留当前 Vue 前端用户体验和主要功能的前提下，把当前 Node Client Gateway 的能力迁移为统一 FastAPI 项目中的 CLIENT 模块，使项目未来能够与其他四组后端模块合并为一个统一 FastAPI 后端项目。同时，在其他组真实服务尚未接入前，本地仍必须能够完整跑通登录、行情、资产、买入、卖出、撤单、成交、提醒、安全设置等流程。

一、最终目标架构

请将当前“Vue 前端 + Node Client Gateway + 多个 mock 服务”的结构，逐步改造为：

```text
Vue 前端 :5173
  ↓
FastAPI 统一后端 :8000
  ├── /api/client/*          CLIENT 交易客户端模块，替代原 Node Gateway
  ├── /api/v1/account/*      ACCOUNT 模块接口；本地阶段由 mock router 实现
  ├── /api/v1/trade/*        TRADE 模块接口；本地阶段由 mock router 实现
  ├── /api/v1/info/*         INFO 模块接口；本地阶段由 mock router 实现
  └── /api/v1/admin/*        ADMIN 模块接口；本地阶段由 mock router 实现
```

最终合并时：

```text
/api/v1/account/* 由账户组真实实现替换 mock account router
/api/v1/trade/*   由中央交易组真实实现替换 mock trade router
/api/v1/info/*    由信息发布组真实实现替换 mock info router
/api/v1/admin/*   由交易管理组真实实现替换 mock admin router
/api/client/*     由交易客户端组维护，作为前端 BFF / 客户端后端模块
```

二、关键原则

1. 不再将 Node Client Gateway 作为最终后端服务。
2. 当前 Node Gateway 中的认证编排、证书逻辑、客户端自有数据、资产聚合、下单编排、错误处理、字段映射等能力，需要迁移到 FastAPI 的 CLIENT 模块。
3. 前端不要直接调用 `/api/v1/account`、`/api/v1/trade`、`/api/v1/info`、`/api/v1/admin`。
4. 前端统一调用 `/api/client/*`，由 CLIENT 模块再调用 ACCOUNT、TRADE、INFO、ADMIN 模块。
5. 本地开发阶段，ACCOUNT、TRADE、INFO、ADMIN 由同一个 FastAPI 项目中的 mock routers 实现，不需要启动多个 mock 端口。
6. 未来合并阶段，只替换 `/api/v1/*` 下的 mock routers 为真实 routers 或真实服务调用，`/api/client/*` 和前端尽量不改。
7. SQLite 或本地轻量存储只保存 CLIENT 自有数据：客户端权限申请、证书公钥、登录记录、价格提醒、通知、自选股、偏好设置。
8. 资金、持仓、委托、成交、行情、涨跌停、停牌状态等权威数据，不应作为 CLIENT 数据持久化；本地 mock 阶段可以在 mock routers 中模拟。
9. 接口 V2 规范中的字段、枚举、错误码、响应格式必须作为最终后端统一规范。

三、统一 FastAPI 项目目录建议

请新增或整理为如下结构：

```text
backend_fastapi/
  main.py
  core/
    config.py
    response.py
    errors.py
    security.py
    request_context.py
    cache.py
  client/
    router.py
    schemas.py
    service.py
    certificate_service.py
    state_service.py
    account_aggregation_service.py
    trade_orchestration_service.py
    result_sync_service.py
    database.py
    models.py
    repositories.py
    adapters/
      account_adapter.py
      trade_adapter.py
      info_adapter.py
      admin_adapter.py
    mappers.py
  mock_modules/
    account_router.py
    trade_router.py
    info_router.py
    admin_router.py
    seed_data.py
    mock_store.py
  tests/
    test_client_login.py
    test_client_order.py
    test_client_account.py
```

其中：

* `backend_fastapi/main.py` 创建 FastAPI app，挂载所有 routers。
* `client/router.py` 暴露 `/api/client/*`，替代当前 Node Gateway 的 `/api/*`。
* `mock_modules/*_router.py` 暴露 `/api/v1/account/*`、`/api/v1/trade/*`、`/api/v1/info/*`、`/api/v1/admin/*`，模拟其他组接口。
* `client/adapters/*.py` 通过 HTTP 或 app 内部 service 调用 `/api/v1/*`。本地阶段可以直接调用 mock service 函数，integration 阶段可改为 HTTP 调用真实服务。
* `core/response.py` 统一响应格式。
* `core/errors.py` 统一错误码。
* `core/cache.py` 实现简单 TTL 内存缓存。

四、统一响应格式

所有 `/api/v1/*` 接口严格按照《接口V2_修正版.md》返回：

```json
{
  "success": true,
  "code": "OK",
  "message": "success",
  "data": {},
  "request_id": "req-20260525-0001",
  "timestamp": "2026-05-25T10:30:00+08:00"
}
```

失败响应：

```json
{
  "success": false,
  "code": "TRADE_E05",
  "message": "价格非法或超出涨跌停范围",
  "data": null,
  "request_id": "req-20260525-0001",
  "timestamp": "2026-05-25T10:30:00+08:00"
}
```

`/api/client/*` 为了兼容当前前端，可以返回当前前端更容易使用的格式：

```json
{
  "ok": true,
  "code": "OK",
  "message": "success",
  "data": {}
}
```

或者兼容旧字段：

```json
{
  "ok": true,
  "order": {},
  "fills": []
}
```

但内部必须保留 `code` 和 `message`，不要再只返回 message。

五、字段规范

外部统一接口 `/api/v1/*` 必须使用 snake_case：

```text
fund_account_id
security_account_id
investor_id
stock_code
stock_name
latest_price
best_bid_price
best_ask_price
available_amount
frozen_amount
available_quantity
frozen_quantity
order_id
order_price
order_quantity
filled_quantity
remaining_quantity
avg_fill_price
submitted_at
```

前端当前如果使用 camelCase 或业务字段：

```text
symbol
name
lastPrice
bid
ask
available
frozen
availableShares
frozenShares
filledQuantity
avgPrice
```

请在 `client/mappers.py` 中做映射，不要让 Vue 页面直接处理 V2 snake_case 字段。

六、保留前端体验，但调整 API Base

当前前端可以继续使用统一 API 封装，但 base URL 改为：

```env
VITE_CLIENT_API_BASE=http://localhost:8000/api/client
```

前端内部接口建议改成或映射为：

认证：

* `POST /auth/login`
* `POST /auth/enroll`
* `POST /auth/verify`
* `POST /auth/rebind`
* `GET /auth/me`

客户端私有功能：

* `POST /client/applications`
* `GET /client/applications`
* `GET /client/login-records`
* `POST /client/login-records`
* `GET /client/alerts`
* `POST /client/alerts`
* `PATCH /client/alerts/{id}`
* `DELETE /client/alerts/{id}`
* `GET /client/notifications`
* `PATCH /client/notifications/{id}/read`
* `GET /client/watchlist`
* `POST /client/watchlist/{symbol}/toggle`
* `GET /client/preferences`
* `PATCH /client/preferences`

账户：

* `GET /account/summary`
* `GET /account/funds`
* `GET /account/holdings`
* `GET /account/cash-flows`
* `GET /account/stock-flows`
* `POST /account/funds/deposit`
* `POST /account/funds/withdraw`
* `POST /account/passwords/trade`
* `POST /account/passwords/withdraw`

行情：

* `GET /market/stocks`
* `GET /market/quotes`

交易：

* `GET /trade/orders`
* `POST /trade/orders`
* `POST /trade/orders/{id}/cancel`
* `GET /trade/fills`

注意：这些是前端到 CLIENT 模块的接口，不是五组统一外部接口。CLIENT 模块内部再调用 `/api/v1/*`。

七、实现 `/api/v1/account/*` mock router

在 `mock_modules/account_router.py` 中实现接口文档中的核心 ACCOUNT 接口：

1. `POST /api/v1/account/auth/login`

   * 请求：

     ```json
     {
       "fund_account_id": "FUND000001",
       "password": "123456",
       "client_time": "2026-05-25T10:30:00+08:00"
     }
     ```
   * 响应 data：

     ```json
     {
       "verified": true,
       "investor_id": "INV000001",
       "fund_account_id": "FUND000001",
       "security_account_id": "SEC000001",
       "first_login": false,
       "token": "mock-jwt-token",
       "expires_at": "2026-05-25T18:30:00+08:00"
     }
     ```

2. `POST /api/v1/account/auth/password`

   * 支持 `password_type = TRADE / WITHDRAW`

3. `GET /api/v1/account/fund-accounts/{fund_account_id}`

   * 返回 `available_amount`、`frozen_amount`、`total_amount`、`status`

4. `POST /api/v1/account/fund-accounts/{fund_account_id}/freeze`

   * 买入前冻结资金
   * 校验 available_amount
   * 不足返回 `ACCOUNT_INSUFFICIENT_FUNDS`

5. `POST /api/v1/account/fund-accounts/{fund_account_id}/release`

   * 撤单或提交失败释放冻结资金

6. `POST /api/v1/account/fund-accounts/{fund_account_id}/settlements`

   * mock 阶段可用于成交扣款/入账

7. `GET /api/v1/account/security-accounts/{security_account_id}/positions`

   * 返回 `SecurityPosition[]`

8. `POST /api/v1/account/security-accounts/{security_account_id}/positions/freeze`

   * 卖出前冻结可卖持仓
   * 不足返回 `ACCOUNT_INSUFFICIENT_POSITION`

9. `POST /api/v1/account/security-accounts/{security_account_id}/positions/release`

   * 撤单或提交失败释放持仓

10. `POST /api/v1/account/security-accounts/{security_account_id}/positions/settlements`

    * mock 阶段可用于成交后增加/扣减持仓

11. `GET /api/v1/account/associations/check`

    * 校验资金账户和证券账户一对一绑定，返回 `is_related`、`allow_operation`、账户状态等

八、实现 `/api/v1/trade/*` mock router

在 `mock_modules/trade_router.py` 中实现：

1. `POST /api/v1/trade/orders`

   * 请求字段严格按接口 V2：

     ```json
     {
       "client_order_id": "CLI202605250001",
       "investor_id": "INV000001",
       "fund_account_id": "FUND000001",
       "security_account_id": "SEC000001",
       "stock_code": "600001",
       "side": "BUY",
       "order_price": "10.10",
       "order_quantity": 100,
       "submitted_at": "2026-05-25T10:30:00+08:00"
     }
     ```
   * 校验：

     * 字段缺失：`TRADE_E01`
     * 股票不存在：`TRADE_E02`
     * 股票暂停或收盘：`TRADE_E03`
     * 买卖方向非法：`TRADE_E04`
     * 价格非法或超涨跌停：`TRADE_E05`
     * 数量非法：`TRADE_E06`
   * 返回：

     ```json
     {
       "order_id": "ORD202605250001",
       "status": "ACCEPTED",
       "accepted_at": "..."
     }
     ```

2. `GET /api/v1/trade/orders`

   * 支持 `investor_id`、`page`、`page_size`

3. `GET /api/v1/trade/orders/{order_id}`

4. `POST /api/v1/trade/orders/{order_id}/cancel`

   * 只有 `QUEUED` 或 `PARTIALLY_FILLED` 可撤
   * 其他状态返回 `TRADE_E07`
   * 返回 `cancelled_quantity`、`released_resource_type`、`released_amount` 或 `released_quantity`

5. `GET /api/v1/trade/market/{stock_code}`

   * 返回 `MarketQuote`

6. `WS /api/v1/trade/ws/orders`

   * 可选第一版实现
   * 如果暂不实现 WebSocket，CLIENT 模块可以通过 `/api/client/trade/fills` 从 mock store 查询成交回报

mock trade 可以简化撮合：允许一部分订单立即成交，一部分订单保持 `QUEUED` 以便测试撤单。

九、实现 `/api/v1/info/*` mock router

在 `mock_modules/info_router.py` 中实现：

1. `GET /api/v1/info/stocks?keyword=...`

   * 支持股票代码精确查询和名称模糊查询
   * 返回 `stock_code`、`stock_name`

2. `GET /api/v1/info/stocks/{stock_code}/quote`

   * 返回 `MarketQuote`
   * 包括：

     * `stock_code`
     * `stock_name`
     * `latest_price`
     * `open_price`
     * `high_price`
     * `low_price`
     * `volume`
     * `best_bid_price`
     * `best_ask_price`
     * `updated_at`

3. `GET /api/v1/info/stocks/{stock_code}/klines`

   * 可选，VIP 功能，不影响 CLIENT 主流程

十、实现 `/api/v1/admin/*` mock router

在 `mock_modules/admin_router.py` 中实现：

1. `GET /api/v1/admin/stocks/{stock_code}/quote`

   * 返回股票行情与涨跌停信息，或从 mock trade/info 共享数据读取

2. `GET /api/v1/admin/stocks/{stock_code}/rule`

   * 返回：

     ```json
     {
       "stock_code": "600001",
       "stock_status": "OPEN",
       "limit_up_price": "11.00",
       "limit_down_price": "9.00"
     }
     ```

3. `GET /api/v1/admin/trading-day/status`

   * 返回交易日状态，例如 `OPEN` 或 `CLOSED`

也可以在 mock 阶段由 `/api/v1/trade/market/{stock_code}` 提供最终涨跌停和状态，但 CLIENT 的 admin_adapter 要预留独立接口。

十一、实现 CLIENT 模块 `/api/client/*`

将当前 Node Gateway 逻辑迁移为 FastAPI：

1. 登录 `POST /api/client/auth/login`

   * 前端传 `{ account, password }`
   * 支持当前测试账号 `admin / 123456`
   * 内部将 account 映射为 `fund_account_id`

     * 若 account 是 `admin`，映射为 `FUND000001`
     * 若 account 本身是 `FUND000001`，直接使用
   * 检查本地 client_users 是否开通客户端权限
   * 未开通返回：

     ```json
     {
       "ok": false,
       "code": "CLIENT_ACCESS_REQUIRED",
       "action": "apply",
       "message": "未开通客户端权限，请先申请"
     }
     ```
   * 已开通则调用 `/api/v1/account/auth/login`
   * 登录成功后保存 session：`token`、`investor_id`、`fund_account_id`、`security_account_id`
   * 再处理证书流程：

     * 无证书或 first_login 返回 `action=enroll`
     * 有证书返回 `action=verify + challenge`

2. 证书绑定 `POST /api/client/auth/enroll`

   * 保存 public_key 到 CLIENT 数据库
   * 私钥仍由前端 IndexedDB 保存
   * 返回 token + user

3. 证书验证 `POST /api/client/auth/verify`

   * 使用 public_key 验证 signature
   * 成功后返回 token + user

4. 客户端权限申请

   * `POST /api/client/client/applications`
   * 调 ACCOUNT 登录或查询账户，确认账户存在
   * 写入 client_users 和 client_applications
   * first_login=true

5. 资产摘要 `GET /api/client/account/summary`

   * 从 session 取 fund_account_id/security_account_id
   * 调 ACCOUNT fund account
   * 调 ACCOUNT positions
   * 调 INFO quote 补行情
   * 返回前端需要的：

     ```json
     {
       "ok": true,
       "account": "admin",
       "available": 200000,
       "frozen": 0,
       "marketValue": 14853,
       "totalEquity": 214853
     }
     ```

6. 持仓 `GET /api/client/account/holdings`

   * 调 ACCOUNT positions
   * 调 INFO quote
   * 映射为前端字段：

     * `stock_code` -> `symbol`
     * `stock_name` -> `name`
     * `total_quantity` -> `shares`
     * `available_quantity` -> `availableShares`
     * `frozen_quantity` -> `frozenShares`
     * `cost_price` -> `costPrice`
     * `latest_price` -> `lastPrice`

7. 行情 `GET /api/client/market/stocks`

   * 调 INFO `/stocks` 与 `/quote`
   * 返回前端字段：

     * `symbol`
     * `name`
     * `lastPrice`
     * `bid`
     * `ask`
     * `dayHigh`
     * `dayLow`
     * `limitUp`
     * `limitDown`
     * `status`

8. 买入/卖出 `POST /api/client/trade/orders`

   * 前端传：

     ```json
     {
       "account": "admin",
       "symbol": "600001",
       "side": "买入",
       "price": 10.1,
       "quantity": 100,
       "note": ""
     }
     ```
   * 转换为 V2：

     * side: `买入` -> `BUY`，`卖出` -> `SELL`
     * price -> decimal string
     * symbol -> stock_code
   * 下单前必须：

     1. 从 session 获取 investor_id、fund_account_id、security_account_id
     2. 调 ACCOUNT `/associations/check`
     3. 调 ADMIN rule 或 TRADE market 获取 `stock_status`、`limit_up_price`、`limit_down_price`
     4. 校验股票状态、价格、数量
     5. 买入：调 ACCOUNT freeze funds
     6. 卖出：调 ACCOUNT freeze position
     7. 调 TRADE submit order
     8. 如果 TRADE 提交失败，补偿 release
     9. 返回前端当前格式 `{ ok:true, order:{...}, fills:[] }`
   * 不允许只依赖前端校验。

9. 撤单 `POST /api/client/trade/orders/{id}/cancel`

   * 调 TRADE cancel
   * mock 阶段可由 mock trade/account 完成释放；CLIENT 刷新缓存即可
   * 返回中文状态

10. 成交 `GET /api/client/trade/fills`

    * 如果 WebSocket 未实现，从 mock trade store 返回成交记录
    * 如果 WebSocket 实现，从 ResultCache 返回
    * 前端接口保持可用

十二、CLIENT 自有数据库

在 FastAPI 中用 SQLite 保存 CLIENT 自有数据。可以使用 SQLAlchemy + SQLite，或简单 sqlite3，建议 SQLAlchemy。

表：

```text
client_users
client_certificates
client_sessions
client_login_records
client_applications
client_alerts
client_notifications
client_preferences
client_watchlist
```

不要保存：

```text
fund_accounts
security_positions
trade_orders
trade_records
market_quotes
```

这些在 mock 阶段可以放在 `mock_store.py`，正式阶段由其他模块数据库维护。

十三、缓存层

实现轻量内存 TTL 缓存：

1. SessionCache

   * key: account
   * value: token、investor_id、fund_account_id、security_account_id、expires_at

2. MarketCache

   * TTL 5 秒
   * key: quote:{stock_code}、rule:{stock_code}

3. AccountCache

   * TTL 2-5 秒
   * key: account_summary:{account}
   * 下单、撤单、成交、存取款后失效

4. OrderCache / ResultCache

   * TTL 2-5 秒或 session 期间
   * 下单、撤单、成交回报后刷新

不要缓存密码验证、修改密码、冻结、释放、下单、撤单这些写操作。

十四、错误码体系

至少实现并在前端可展示：

```text
COMMON_BAD_REQUEST
COMMON_UNAUTHORIZED
COMMON_NOT_FOUND
COMMON_CONFLICT
COMMON_INTERNAL_ERROR
AUTH_BAD_CREDENTIALS
AUTH_CERT_REQUIRED
AUTH_CERT_INVALID
CLIENT_ACCESS_REQUIRED
TRADE_E01
TRADE_E02
TRADE_E03
TRADE_E04
TRADE_E05
TRADE_E06
TRADE_E07
ACCOUNT_INSUFFICIENT_FUNDS
ACCOUNT_INSUFFICIENT_POSITION
ACCOUNT_STATUS_BLOCKED
EXTERNAL_SERVICE_ERROR
```

CLIENT 接口对前端返回：

```json
{
  "ok": false,
  "code": "ACCOUNT_INSUFFICIENT_FUNDS",
  "message": "可用资金不足"
}
```

十五、启动方式

新增后端启动：

```bash
pip install fastapi uvicorn pydantic sqlalchemy python-multipart
uvicorn backend_fastapi.main:app --reload --port 8000
```

前端启动：

```bash
npm run dev
```

`.env`：

```env
VITE_CLIENT_API_BASE=http://localhost:8000/api/client
API_PROFILE=local_unified
ENABLE_TRADE_WS=false
```

如果保留 Node Gateway 作为 legacy，可以不默认启动。新方案默认只需要：

```text
Vue 前端 :5173
FastAPI 后端 :8000
```

十六、迁移策略

请分阶段完成，不要一次性破坏当前前端：

阶段 1：

* 新建 FastAPI 统一后端框架
* 实现 mock `/api/v1/account`、`/api/v1/trade`、`/api/v1/info`、`/api/v1/admin`
* 实现 `/health` 和 `/docs`

阶段 2：

* 实现 `/api/client/*`，迁移 Node Gateway 的主要接口
* 前端 `VITE_CLIENT_API_BASE` 切换到 `http://localhost:8000/api/client`
* 保证登录、首页、行情、资产、交易、委托、提醒能跑通

阶段 3：

* 补齐资金冻结、持仓冻结、撤单释放、错误码
* 增加缓存和字段映射

阶段 4：

* 可选实现 WebSocket
* 为未来替换真实模块预留 adapters 配置

十七、不要做的事

1. 不要让 Vue 前端直接调用 `/api/v1/account`、`/api/v1/trade`、`/api/v1/info`、`/api/v1/admin`。
2. 不要继续依赖四个旧 mock 端口作为默认启动方式。
3. 不要把资金、持仓、委托、成交、行情写入 CLIENT SQLite。
4. 不要只在前端校验交易规则。
5. 不要在 CLIENT 模块实现真实撮合。
6. 不要在真实 integration 模式下由 CLIENT 私自结算资金和证券。
7. 不要破坏当前前端页面和主要交互体验。

十八、验收标准

完成后必须满足：

1. 本地只需启动 Vue 前端和 FastAPI 后端即可跑通主流程。
2. FastAPI 后端 `/docs` 能看到 `/api/client/*` 和 `/api/v1/*` 接口。
3. 前端能完成：

   * 客户端权限申请
   * 登录
   * 证书绑定/验证/重绑
   * 行情查询
   * 资产查询
   * 买入
   * 卖出
   * 撤单
   * 成交查看
   * 价格提醒
   * 修改密码
   * 登录记录
4. 买入前会冻结资金。
5. 卖出前会冻结持仓。
6. 撤单能释放未成交部分资源。
7. 资金不足、持仓不足、价格越界、股票暂停、数量非法等错误有明确 code/message。
8. 将来其他组真实 `/api/v1/*` routers 合并后，主要替换 mock routers，不需要重写前端。
