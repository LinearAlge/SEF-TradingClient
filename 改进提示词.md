你现在是我的前端开发助手，请在现有 Vue 3 + TypeScript 项目中，基于当前交易客户端页面做一次“成熟交易客户端化”的页面组织、视觉层级和交互重构。重点是让页面不再像 PPT 式标题/数字罗列，而是像一个可操作、状态清晰、交易闭环完整的股票交易客户端。请尽量复用现有代码、现有组件、现有 CSS 变量和统一风格，不要引入大型 UI 框架、复杂图表库、WebSocket 或真实复杂撮合逻辑。

## 一、当前项目背景

我当前负责“交易客户端”部分，已有这些页面和组件：

* `LoginView.vue`
* `DashboardView.vue`
* `MarketView.vue`
* `TradeView.vue`
* `OrdersView.vue`
* `AccountView.vue`
* `AlertsView.vue`
* `SettingsView.vue`
* `App.vue`
* `TopBar.vue`
* `SidebarNav.vue`
* `StatCard.vue`
* `FundsSummary.vue`
* `HoldingsTable.vue`
* `MarketTable.vue`
* `StockDetailCard.vue`
* `StockSearchPanel.vue`
* `PriceTicker.vue`
* `OrderListTable.vue`

项目当前有统一的黑白灰简洁风格，`App.vue` 里有全局 `.card`、`.btn`、`.table`、`.chip`、`.field`、`.input`、`.grid` 等样式。请不要推翻这个风格，但要把视觉从“PPT 式大卡片 + 大标题 + 静态数字”改成“更像真实交易终端的密集信息、明确状态、可操作表格、连续交易流程”。

已有测试后端大致如下，请优先兼容已有接口，不要强迫我现在补很多后端：

* 登录/证书相关：`http://localhost:3001`
* 持仓：`http://localhost:3002/holdings?account=...`
* 资金：`http://localhost:3003/funds?account=...`
* 行情：`http://localhost:3004/stocks?...`

如果某些功能现在没有后端，例如委托、撤单、提醒、成交回报，可以先做前端本地状态或静态 fallback 数据，但交互流程和状态表达要完整，方便后续接真实后端。

## 二、总体目标

请完成一次前端结构优化，目标是：

1. 保留当前统一黑白灰风格，但增强数据密度、操作感和状态反馈。
2. 页面不再只是展示标题和数字，而要体现交易软件的任务流：

   * 看行情
   * 查看资金/持仓
   * 下买入/卖出委托
   * 查看委托状态
   * 撤单
   * 查看成交结果
   * 资产更新
   * 设置价格提醒
   * 修改密码/证书管理
3. 不要做过度复杂功能。不要做真实撮合、复杂 K 线、真实 WebSocket、复杂权限系统。
4. 重点改页面组织、组件职责、交互闭环、空状态/加载态/错误态、数据表格体验。
5. 修改后项目应能正常运行，TypeScript 不报错，组件职责更清楚。

## 三、信息架构调整

请把当前左侧导航和页面命名调整得更像客户端：

* `总览` 可以改成 `首页`
* `行情` 保留为 `行情`
* `下单` 可以改成 `交易`
* `委托` 改成 `委托成交`
* `账户` 改成 `资产`
* `提醒` 保留为 `提醒`
* `设置` 改成 `安全`

对应页面仍然可以使用原来的路由，不一定要改路由路径，但导航文案和页面标题建议调整。

侧边栏底部保留交易日信息，但请加上更真实的状态字段，例如：

* 交易日：2026-05-25
* 交易时段：09:30 - 15:00
* 行情：正常
* 交易通道：正常

## 四、全局视觉改造要求

请优先修改 `App.vue` 的全局样式和相关组件样式，使整体更像成熟客户端，而不是 PPT：

### 1. 卡片风格

当前 `.card` 太像一块一块独立 PPT 内容。请调整为更轻、更密集的面板风格：

* 保持方角或极小圆角，符合组内风格。
* 减少大面积空白。
* 标题区更像工具栏：左侧标题/说明，右侧操作按钮或状态。
* 卡片内部多用表格、状态条、信息行，而不是一堆大号数字。
* 可以新增 `.panel`、`.panel-header`、`.panel-title`、`.panel-subtitle`、`.panel-actions`，但也可以直接增强 `.card`。

### 2. 表格风格

请增强 `.table`：

* 数字列右对齐，新增 `.numeric` 类。
* 表头更清晰，行高适中。
* 行 hover 有浅色背景。
* 选中行有左侧黑色边线或浅灰底。
* 状态不要只用文字颜色，新增状态标签样式，例如 `.status-pill`。
* 表格空态、加载态、错误态要统一。

### 3. 按钮层级

当前黑色按钮太多，导致所有操作都像主操作。请统一：

* 主操作：`.btn-primary`，例如提交委托、登录、保存。
* 次操作：`.btn-ghost` 或普通 `.btn`，例如刷新、重置、预览。
* 危险操作：新增 `.btn-danger` 或 `.danger-action`，例如撤单、批量撤单。
* 小按钮：新增 `.btn-small`，用于表格行内操作。

### 4. 数据密度和层级

请增加这些通用样式：

* `.metric-strip`：横向紧凑指标条，用于展示可用资金、冻结资金、可买/可卖等。
* `.meta-row`：小号状态信息行。
* `.inline-actions`：行内操作按钮容器。
* `.empty-state`：空态。
* `.form-hint`：表单说明和校验提示。
* `.price-up`、`.price-down`、`.price-flat`：涨跌状态。
* `.layout-split`：左侧主内容，右侧辅助信息栏。

注意：国内股票软件通常红涨绿跌，但当前项目是绿涨红跌。如果不想改颜色习惯，至少保证全项目一致，并在 CSS 命名上使用 `price-up/price-down`，不要混乱。

## 五、TopBar 改造

请修改 `TopBar.vue`：

1. 搜索框 placeholder 改成：`搜索股票代码、名称或委托号`
2. 顶部右侧除了 slot actions 外，增加一行或一组小状态：

   * 当前资金账号：从 `localStorage.getItem('trading-account') || 'admin'`
   * 行情连接：正常
   * 交易通道：正常
3. 不需要真的实现全局搜索逻辑，但视觉上要像交易客户端的全局工具栏。
4. 顶部标题不要占太大垂直空间，减少 PPT 感。

## 六、DashboardView：首页/工作台重构

当前首页有三张大指标卡、持仓、市场脉动、最新委托。请改成更像“交易工作台”：

### 需要保留

* 可用资金
* 冻结资金
* 资产总值
* 持仓表
* 市场快照
* 最新委托

### 建议新增或调整

1. 增加 `今日待办` 小面板：

   * 未成交委托
   * 部分成交
   * 可撤单
   * 已触发提醒
     这些可以先用静态数据或从本地 mock 计算。

2. 增加 `最新成交` 小表或列表：

   * 成交时间
   * 股票
   * 方向
   * 成交价
   * 成交量
     没有后端时使用静态 fallback 数据。

3. 持仓表行增加操作入口：

   * 买入
   * 卖出
   * 详情
   * 提醒
     操作可以先只是按钮或跳转，不要求真实提交。

4. 首页布局建议：

   * 顶部：紧凑资产指标条
   * 中部：持仓主表 + 右侧今日待办/市场快照
   * 下部：最新委托 + 最新成交

目标是让首页体现“我现在能做什么”，而不是只展示几个数字。

## 七、MarketView：行情中心重构

当前行情页已有 `StockSearchPanel`、`MarketTable`、`StockDetailCard`、`PriceTicker`，结构基本可以保留，但要增强交互感。

### StockSearchPanel

请改进：

1. 保留代码/名称输入、板块、区间。
2. 刷新方式不要用只读 input，改成更像控件：

   * 手动刷新
   * 自动刷新 5 秒
     当前可以只做 UI，自动刷新可以暂时不真的实现，或只保留定时更新时间。
3. 查询按钮、重置按钮保留。
4. 输入框回车触发查询。
5. 可以增加“仅看自选”“正常交易”这类轻量筛选按钮，但不要求接后端。

### MarketTable

请改进：

1. 表格列建议：

   * 代码/名称
   * 最新价
   * 涨跌幅
   * 最高
   * 最低
   * 买一
   * 卖一
   * 成交量
   * 操作
2. 如果后端没有涨跌幅字段，可以前端用 fallback 或显示 `--`，不要阻塞。
3. 点击行后选中该股票，当前选中行要有明显视觉。
4. 操作列包含：

   * 买入
   * 卖出
   * 提醒
     这些可以先按钮，不要求真实路由联动，但最好 emit 事件或预留 handler。

### StockDetailCard

现有详情已经包含指导书要求的大部分字段，请继续增强：

* 股票代码/名称
* 板块
* 最新价
* 买一/卖一
* 当日最高/最低
* 本周最高/最低
* 本月最高/最低
* 重要公告

请新增几个成熟客户端常见但轻量的区域：

1. `价格边界`

   * 涨停价
   * 跌停价
     如果后端没有，按 `lastPrice * 1.1` / `lastPrice * 0.9` 或显示 `--`。

2. `快捷操作`

   * 买入
   * 卖出
   * 设置提醒

3. `简化盘口`

   * 买一、卖一已经有，可以用小型双列展示，不需要五档盘口。

不要引入复杂 K 线库。可以加一个“走势占位区”或“简化 K 线占位”，但不是必须。

## 八、TradeView：交易页重构

当前 `TradeView` 左右并排两个 `TradeTicket`，很像表单展示。请改成更成熟的单页交易台。

### 结构建议

请将买入/卖出从左右两栏改为 Tab 或 Segment 控件：

* 买入
* 卖出

左侧是下单表单，右侧是当前股票行情与账户约束信息。

### 下单表单字段

保留或实现：

* 股票代码
* 委托类型：限价
* 委托价格
* 委托数量
* 有效期：当日有效
* 备注

### 股票代码联动

输入股票代码后，如果能从行情接口查到该股票，则自动展示：

* 股票名称
* 最新价
* 买一
* 卖一
* 涨停价
* 跌停价
* 状态：正常/停牌/未知

如果当前没有对应后端能力，可以用已有行情列表或静态 fallback。

### 买入校验

买入时：

* 显示可用资金。
* 根据价格计算最大可买数量。
* 委托金额不能超过可用资金。
* 如果价格超出涨跌停范围，显示红色提示并禁止提交。

### 卖出校验

卖出时：

* 股票必须在持仓中。
* 显示持仓数量和可卖数量。
* 委托数量不能超过可卖数量。
* 如果价格超出涨跌停范围，显示红色提示并禁止提交。

### 委托预览

点击“预览”后出现确认区域或弹窗，展示：

* 方向
* 股票
* 委托价格
* 委托数量
* 预计冻结资金或冻结股票
* 有效期
* 风险提示

不要求复杂 modal 库，可以用条件渲染的确认面板。

### 提交委托

如果没有委托后端：

* 前端本地生成一个委托号，例如 `ORD-${Date.now()}`
* 状态为 `未成交`
* 显示提交成功反馈
* 把该委托加入本地委托列表
* 买入时体现“预计冻结资金”
* 卖出时体现“预计冻结股票”
* 提供“查看委托”按钮或提示

如果已有委托后端，则优先接后端，但不要为了这个改坏页面。

### TradeView 右侧辅助面板

新增或改造为：

* 当前行情
* 价格区间
* 可用资金/冻结资金
* 可买数量/可卖数量
* 最近委托

这样可以明显减少“PPT 表单感”。

## 九、OrdersView：委托成交页重构

当前 `OrderListTable` 是静态数组，且列太少。请将它改造成可复用组件，接收 props，不要在组件内部固定写死订单。

### OrderListTable 改造

类型建议：

```ts
type OrderItem = {
  id: string
  createdAt: string
  symbol: string
  name?: string
  side: '买入' | '卖出'
  price: number
  quantity: number
  filledQuantity: number
  avgPrice?: number
  status: '未成交' | '部分成交' | '已成交' | '已撤单' | '已过期' | '已拒绝'
}
```

显示列：

* 委托号
* 时间
* 股票
* 方向
* 委托价
* 委托量
* 已成交
* 未成交
* 成交均价
* 状态
* 操作

### 状态筛选

OrdersView 顶部筛选从 chip 改成真正可点击的 filter：

* 全部
* 可撤
* 未成交
* 部分成交
* 已成交
* 已撤单
* 已过期

点击筛选后更新表格内容。

### 撤单交互

对 `未成交` 和 `部分成交` 的委托显示“撤单”按钮：

* 点击后出现确认提示。
* 确认后将状态改为 `已撤单`。
* 如果是已成交/已撤单/过期，不显示撤单按钮。
* 如果是部分成交，提示“仅撤销未成交数量”。

不要求真实后端撤单，可以先本地状态修改。

### 成交回报

在 OrdersView 增加一个简单 `成交回报` 面板或 Tab：

* 成交时间
* 委托号
* 股票
* 方向
* 成交价
* 成交数量
* 成交金额

这能体现指导书要求的“显示交易结果”。

## 十、AccountView：资产页重构

当前资产页只有资金和持仓。请增强为：

### 顶部资金概览

用紧凑指标条展示：

* 可用资金
* 冻结资金
* 证券市值
* 总资产
* 可取资金
* 账户状态

如果后端没有可取资金和账户状态，可以 fallback：

* 可取资金 = 可用资金
* 账户状态 = 正常

### 持仓表增强

`HoldingsTable` 增加列：

* 代码/名称
* 持仓数量
* 可卖数量
* 冻结数量
* 成本价
* 最新价
* 市值
* 盈亏
* 盈亏率
* 操作

如果后端没有可卖/冻结/市值，可在前端计算或显示 `--`。

### 流水面板

增加两个轻量表格或一个 Tab：

* 资金流水
* 证券流水

没有后端时使用静态 fallback 数据即可。目标是让资产变动逻辑可被看到，例如：

* 买入冻结
* 撤单解冻
* 买入成交扣款
* 卖出成交入账
* 买入入股
* 卖出出股

## 十一、AlertsView：提醒页重构

当前提醒页可以保留，但请让它更像实际提醒管理。

### 提醒列表

字段：

* 股票
* 条件
* 当前价
* 触发价
* 状态：监控中/已暂停/已触发
* 最近触发时间
* 操作：暂停/恢复/编辑/删除

如果没有后端，使用前端本地数组。

### 新增提醒

点击“新增提醒”显示内联表单或简单弹窗：

* 股票代码
* 条件：高于/低于
* 触发价格
* 有效期：当日/本周/长期
* 提醒方式：站内消息

提交后加入列表。

### 市场快照

右侧 `PriceTicker` 可以保留，但请尽量改成接收行情数据 props，或者至少保持样式与其他面板一致。

## 十二、SettingsView：安全页重构

当前页面只有两个密码表单。请改成更像安全中心：

### 分区

* 交易密码
* 取款密码
* 安全证书
* 登录记录

可以用卡片，不一定要 Tab。

### 密码表单

保留当前密码、新密码、确认密码，增加：

* 新旧密码不能相同的前端校验。
* 两次输入必须一致。
* 密码长度提示。
* 修改成功/失败提示。
* 保存按钮放在每个表单内部，不要只有顶部一个“保存”。

如果没有后端，可以只做前端校验和成功提示。

### 安全证书

展示：

* 当前证书状态
* 本机是否已绑定
* 重新绑定证书按钮
* 验证证书按钮

可以复用 `LoginView` 里已有的 certificateStore 思路，但不要重写复杂证书逻辑。

### 登录记录

使用静态 fallback 数据即可：

* 时间
* 方式
* 设备
* 状态

## 十三、LoginView 改造

当前登录页已经有证书逻辑，保留主逻辑，不要破坏。

请优化视觉和流程表达：

1. 普通登录仍然包含：

   * 账户卡号
   * 交易密码
   * 证书状态
   * 进入工作台

2. 重新绑定证书保留，但样式改得更像一个展开的安全验证面板。

3. 首次登录/证书绑定流程要在界面文案上体现：

   * 如果后端返回 enroll，提示“首次登录，将为本机绑定安全证书”。
   * 如果 verify，提示“正在验证本机证书”。

4. 登录按钮 loading、错误、成功提示要清楚。

5. 登录页左侧宣传文案可以减少一点，不要太像产品海报。更像交易终端入口：

   * 资金账户登录
   * 本机证书校验
   * 行情与交易通道状态

## 十四、组件职责调整建议

请重点处理这些当前代码问题：

1. `OrderListTable.vue`

   * 不要内部写死订单。
   * 改为 props 输入订单数组。
   * 支持 compact 模式和 full 模式。
   * 支持行内撤单事件 emit。

2. `PriceTicker.vue`

   * 不要完全写死数据。
   * 改为可接收 `items` props；没有 props 时使用 fallback 数据。
   * 点击股票可 emit `select`。

3. `MarketTable.vue`

   * 支持选中态。
   * 支持操作列。
   * 数字列右对齐。
   * emit 具体事件：select、buy、sell、alert。

4. `HoldingsTable.vue`

   * 支持操作列。
   * 数字列右对齐。
   * 增加可卖数量、冻结数量、市值等字段的兼容显示。

5. `StatCard.vue`

   * 当前大数字很 PPT，可增加 `density?: 'default' | 'compact'` 或直接减小视觉重量。
   * 允许传入辅助信息行。

6. `TopBar.vue`

   * 增加账户与通道状态。
   * 搜索框文案修正。

7. 如果发现项目里有 `TradeTicket.vue`、`AlertsList.vue`、`AppShell.vue` 等未在我列出的文件里，请也一起按上述目标轻量改造。

## 十五、交互状态要求

所有页面都要尽量包含：

* 加载中
* 错误态
* 空数据
* 成功提示
* 禁用态
* 表单校验提示
* 操作确认

尤其是：

* 提交委托前要预览确认。
* 撤单前要确认。
* 密码修改前端校验。
* 股票价格超出涨跌停范围时禁止提交。
* 买入金额超过可用资金时禁止提交。
* 卖出数量超过可卖数量时禁止提交。

## 十六、不要做的事

请不要做这些：

1. 不要引入 Element Plus、Ant Design、Naive UI 等大型 UI 框架。
2. 不要引入复杂图表库。
3. 不要实现真实撮合系统。
4. 不要强行改后端接口。
5. 不要把所有状态都写成复杂全局 store，除非项目已经有 Pinia/Vuex。
6. 不要为了视觉效果加入大量动画。
7. 不要破坏现有登录和证书逻辑。
8. 不要让页面变成花哨金融大屏，仍然保持组内黑白灰统一风格。

## 十七、建议实现顺序

请按以下顺序修改，保证每一步都能运行：

1. 先改全局 CSS：卡片、表格、按钮、状态标签、数字列、空态。
2. 改 `TopBar` 和 `SidebarNav`，统一信息架构和状态显示。
3. 改 `OrderListTable`、`PriceTicker`、`MarketTable`、`HoldingsTable` 这些基础组件。
4. 改 `DashboardView`，把首页变成工作台。
5. 改 `MarketView`，强化行情查询和个股详情。
6. 改 `TradeView`，做买入/卖出 Tab、校验、预览、提交结果。
7. 改 `OrdersView`，做筛选、撤单、成交回报。
8. 改 `AccountView`，增加流水和更完整持仓字段。
9. 改 `AlertsView`，做本地提醒管理。
10. 改 `SettingsView` 和 `LoginView` 的视觉与提示。

## 十八、验收标准

修改完成后请确保：

1. 页面整体仍是统一黑白灰风格。
2. 视觉更像交易客户端，而不是 PPT。
3. 每个页面都有明确主任务和操作入口。
4. 行情、交易、委托、资产之间形成基本闭环。
5. 所有表格数字列右对齐。
6. 订单状态使用状态标签。
7. 下单有价格/数量/资金/持仓校验。
8. 撤单有确认。
9. 成交回报有展示位置。
10. 没有真实后端的功能使用本地 fallback，不影响页面运行。
11. TypeScript 不报错。
12. 不引入大型依赖。
13. 不破坏已有登录、证书、资金、持仓、行情接口逻辑。

请先阅读现有代码结构，然后给出一个简短修改计划，再直接实施代码修改。
